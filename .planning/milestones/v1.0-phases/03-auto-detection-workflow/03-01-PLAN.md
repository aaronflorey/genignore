---
phase: 03-auto-detection-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/provider/detectors.go
  - internal/provider/detectors_test.go
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, DET-01]
must_haves:
  truths:
    - "User can run detect and get a deterministic final provider set from project, OS, and installed-tool signals"
    - "Include/exclude controls resolve as detected + include - exclude, with exclude winning last"
    - "Detect hard-fails before template fetch or file write when the final provider set is empty"
    - "Detector results and provider selections remain alphabetically ordered and stable"
  artifacts:
    - path: "internal/app/service.go"
      provides: "Detect orchestration, include/exclude resolution, empty-result guard, deterministic evidence ordering"
    - path: "internal/provider/detectors.go"
      provides: "Project, OS, PATH, and installed-app detector rules with structured evidence"
    - path: "internal/provider/detectors_test.go"
      provides: "Focused detector coverage for signal matching and evidence metadata"
  key_links:
    - from: "internal/app/service.go"
      to: "internal/provider/detectors.go"
      via: "Service iterates detector registry to build detection results and final provider set"
      pattern: "Detectors|DetectionResults|Detect\(ctx"
    - from: "internal/app/service.go"
      to: "internal/api/client.go"
      via: "Detect must stop before FetchTemplate when final selection is empty"
      pattern: "FetchTemplate"
---

<objective>
Harden the core `detect` flow so Phase 3 selection semantics are deterministic, auditable, and safely fail when detection resolves to nothing.

Purpose: Close the gap between the current partial detect implementation and the phase contract for selection control, evidence stability, and empty-result behavior.
Output: Updated service/detector code and regression tests proving deterministic detect behavior across supported signal types.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-auto-detection-workflow/03-CONTEXT.md

<interfaces>
From internal/app/service.go:
```go
func (s *Service) Detect(ctx context.Context, opts DetectOptions) (CommandResult, error)
type DetectOptions struct {
  Include []string
  Exclude []string
  DryRun  bool
  Verbose bool
}
```

From internal/provider/provider.go:
```go
type Result struct {
  Key      string
  Matched  bool
  Reason   string
  Evidence string
  Error    string
}
type Detector interface {
  Detect(ctx context.Context, cwd string) Result
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for detect selection semantics and deterministic evidence ordering</name>
  <files>internal/app/service_test.go</files>
  <read_first>internal/app/service.go, internal/app/service_test.go, internal/app/types.go</read_first>
  <behavior>
    - Test 1: Detect resolves final providers using `detected + include - exclude`, with exclude taking precedence over include.
    - Test 2: Detect returns a hard error and makes no template request when the final provider set is empty.
    - Test 3: Detection results are returned in provider-key order rather than map iteration order.
    - Test 4: Matched providers, included providers, excluded providers, and final providers stay alphabetically sorted.
  </behavior>
  <action>Expand `internal/app/service_test.go` with table-style detect cases that intentionally use unsorted detector maps and mixed include/exclude inputs. Assert exact provider slices, exact empty-result error text, and that `FetchTemplate` is not called when selection resolves to empty.</action>
  <acceptance_criteria>
    - `internal/app/service_test.go` includes coverage for include/exclude precedence and empty-result failure.
    - Detection result ordering is asserted explicitly, not indirectly.
    - `go test ./internal/app -run "TestDetect"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "TestDetect"</automated>
  </verify>
  <done>Detect semantics are proven stable and deterministic before implementation changes are finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused detector coverage for project, OS, PATH, and installed-app signals</name>
  <files>internal/provider/detectors.go, internal/provider/detectors_test.go</files>
  <read_first>internal/provider/detectors.go</read_first>
  <behavior>
    - Test 1: File-based detectors return matched evidence paths when signals exist.
    - Test 2: OS detector returns stable runtime evidence and mismatch reason when not matched.
    - Test 3: PATH/app detectors return matched evidence when binaries/apps are present and stable non-match reasons otherwise.
    - Test 4: Parse/read failures produce structured `Reason`/`Error` metadata rather than panics or silent success.
  </behavior>
  <action>Create `internal/provider/detectors_test.go` with focused tests for the reusable detector helpers and special-case detectors such as React/Laravel. Keep fixtures minimal and local to the tests, and assert `Result` fields directly so DET-01 behavior stays explicit.</action>
  <acceptance_criteria>
    - `internal/provider/detectors_test.go` exists and covers reusable detector helpers plus at least one JSON-parse/read-path detector.
    - Detector tests assert `Key`, `Matched`, `Reason`, `Evidence`, and `Error` where relevant.
    - `go test ./internal/provider` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/provider</automated>
  </verify>
  <done>Detector rules are regression-covered across the supported signal categories that power `detect`.</done>
</task>

<task type="auto">
  <name>Task 3: Make detect execution and evidence ordering deterministic in the service layer</name>
  <files>internal/app/service.go, internal/provider/detectors.go</files>
  <read_first>internal/app/service.go, internal/provider/detectors.go, internal/app/service_test.go, internal/provider/detectors_test.go</read_first>
  <action>Refine the detect orchestration so detector execution and `DetectionResults` serialization are ordered by provider key, include/exclude resolution remains sorted end-to-end, and the empty-result guard fires before any template fetch or file write. Keep the current-directory-only scope and existing detector registry model intact.</action>
  <acceptance_criteria>
    - `internal/app/service.go` no longer relies on Go map iteration order for detect output.
    - `internal/app/service.go` preserves the existing warning behavior for unsupported keys and remote drift.
    - Detect returns before `FetchTemplate` when no providers remain.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app ./internal/provider</automated>
  </verify>
  <done>Detect behavior is deterministic and phase-correct with focused tests proving it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem/PATH/app checks → detector results | Untrusted host state influences automatic provider selection |
| Detector registry → service final selection | Multiple signal sources are merged before API/file side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | Detect include/exclude resolution | mitigate | Keep supported-key sanitization and deterministic set math; cover exclude-over-include precedence with tests |
| T-03-02 | R | Detection result ordering | mitigate | Sort detector keys/results before rendering or serialization; assert order directly in tests |
| T-03-03 | D | Empty detect result path | mitigate | Hard-fail before template fetch/file write; verify no write-side effect in tests |
</threat_model>

<verification>
- Run `go test ./internal/app ./internal/provider`.
- Confirm detect coverage maps directly to CMD-01, CMD-02, CMD-03, CMD-04, and DET-01.
</verification>

<success_criteria>
- Detect selection semantics are deterministic and test-backed.
- Empty final selections fail safely before any API template fetch or write.
- Detector rules return structured, stable evidence across the supported signal categories.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auto-detection-workflow/03-01-SUMMARY.md`
</output>
