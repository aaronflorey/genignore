---
quick: 260412-3md
type: execute
autonomous: true
files_modified:
  - internal/gitignore/manager.go
  - internal/gitignore/manager_test.go
must_haves:
  truths:
    - "Managed output always includes .env and .env.* ignore coverage."
    - "Managed output always includes explicit exceptions for .env.example and .env.ci."
    - "When .gitignore already has .env-related rules, write behavior reconciles them deterministically instead of producing conflicting/duplicate env lines."
  artifacts:
    - path: "internal/gitignore/manager.go"
      provides: "Env-rule enforcement + reconciliation logic in managed block write path"
    - path: "internal/gitignore/manager_test.go"
      provides: "Regression coverage for enforced env defaults, explicit exceptions, and existing-rule reconciliation"
  key_links:
    - from: "UpsertManagedBlock(existing, block)"
      to: "managed block content assembled for write"
      via: "env rule reconciliation against existing .gitignore before final merge"
      pattern: "existing env rules influence final managed env segment deterministically"
---

<objective>
Guarantee secure, deterministic env ignore behavior in generated output by always enforcing env defaults and reconciling existing env-related rules from the current `.gitignore` before writing.

Purpose: prevent accidental secret leakage from missing env ignores while preserving intentional repository-safe exceptions.
Output: tested manager behavior that enforces `.env` and `.env.*`, forces `!.env.example` and `!.env.ci`, and handles pre-existing env rules without churn.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@internal/gitignore/manager.go
@internal/gitignore/manager_test.go

<interfaces>
From `internal/gitignore/manager.go`:

```go
func BuildManagedBlock(providers []string, template string) string
func (m *Manager) UpsertManagedBlock(block string, dryRun bool) (FileAction, error)
func mergeManagedBlock(existing, block string) (string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify env ignore contract with focused failing tests</name>
  <files>internal/gitignore/manager_test.go</files>
  <behavior>
    - Test 1: Generated managed block always contains `.env` and `.env.*` ignore rules.
    - Test 2: Generated managed block always contains `!.env.example` and `!.env.ci` even if template omits them.
    - Test 3: Existing `.gitignore` env lines (duplicates and env-specific variants) are accounted for before write so final output is deterministic and avoids conflicting/duplicate env entries.
    - Test 4: Re-running with equivalent env content is idempotent (no file churn).
  </behavior>
  <action>Add regression tests around managed block build+upsert behavior that lock the exact env segment contract: enforce baseline env ignores, enforce explicit negations for `.env.example`/`.env.ci`, and verify reconciliation when existing `.gitignore` already contains env-related lines (including mixed managed/unmanaged placement).</action>
  <verify>
    <automated>go test ./internal/gitignore -run "TestBuildManagedBlock.*Env|TestUpsert.*Env"</automated>
  </verify>
  <done>Tests fail first on current behavior and unambiguously define required env rule outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement enforced env defaults plus existing-rule reconciliation in merge path</name>
  <files>internal/gitignore/manager.go</files>
  <behavior>
    - `.env` and `.env.*` are always present in managed rules.
    - `!.env.example` and `!.env.ci` are always present as explicit exceptions.
    - Existing `.gitignore` env-related rules are considered during write so output remains deterministic and avoids duplicate/conflicting env lines.
  </behavior>
  <action>Implement a deterministic env-rule normalization step in the managed block pipeline and merge path: inject required baseline env rules + required negations, fold in relevant pre-existing env rules from current `.gitignore` where safe, dedupe in stable order, and keep marker ownership boundaries intact. Do not add new dependencies; keep logic localized to `internal/gitignore/manager.go` helpers.</action>
  <verify>
    <automated>go test ./internal/gitignore -run "TestBuildManagedBlock.*Env|TestUpsert.*Env|TestUpsertReplacesOnlyManagedRegion|TestUpsertIsIdempotentForEquivalentRun"</automated>
  </verify>
  <done>New env tests pass, existing marker-safety behavior still passes, and reruns are byte-stable for equivalent input.</done>
</task>

<task type="auto">
  <name>Task 3: Run package-level regression verification</name>
  <files>internal/gitignore/manager.go, internal/gitignore/manager_test.go</files>
  <action>Run full gitignore package tests to validate no regressions outside the new env behavior and confirm dedupe/merge guarantees remain intact.</action>
  <verify>
    <automated>go test ./internal/gitignore</automated>
  </verify>
  <done>`internal/gitignore` tests are fully green with enforced env behavior and deterministic merge output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote template input -> managed block writer | Untrusted template content influences repository ignore behavior |
| Existing user `.gitignore` -> reconciliation logic | User-authored env rules are interpreted before write and can cause accidental overreach if normalized unsafely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260412-01 | I (Information Disclosure) | env rule generation | mitigate | Always enforce `.env` + `.env.*` and explicit `!.env.example`/`!.env.ci` via tested normalization rules |
| T-260412-02 | T (Tampering) | existing-rule reconciliation | mitigate | Reconcile only env-scoped rules with deterministic dedupe and test coverage for conflict/duplicate handling |
| T-260412-03 | D (Denial of service via churn) | merge/idempotency path | mitigate | Add idempotency regression assertions so equivalent reruns do not rewrite `.gitignore` |
</threat_model>

<verification>
- `go test ./internal/gitignore -run "TestBuildManagedBlock.*Env|TestUpsert.*Env"`
- `go test ./internal/gitignore`
</verification>

<success_criteria>
- Managed output always includes `.env`, `.env.*`, `!.env.example`, and `!.env.ci`.
- Existing `.env*` rules in `.gitignore` are accounted for before write and do not create unstable/conflicting output.
- Managed marker safety and idempotent reruns remain intact.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-3md-ensure-env-and-env-variants-are-always-i/260412-3md-SUMMARY.md`.
</output>
