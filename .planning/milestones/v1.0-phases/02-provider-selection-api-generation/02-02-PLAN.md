---
phase: 02-provider-selection-api-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/cli.go
  - internal/app/cli_test.go
  - internal/app/catalog.go
  - internal/app/catalog_test.go
autonomous: true
requirements: [CMD-06, DET-04]
must_haves:
  truths:
    - "User can list all supported provider keys from the CLI"
    - "User can search provider keys by substring from the CLI"
    - "List/search outputs are alphabetically ordered and stable"
  artifacts:
    - path: "internal/app/cli.go"
      provides: "Cobra list/search command wiring"
    - path: "internal/app/catalog.go"
      provides: "Deterministic provider catalog and filtering logic"
    - path: "internal/app/cli_test.go"
      provides: "CLI command behavior coverage for list/search"
  key_links:
    - from: "internal/app/cli.go"
      to: "internal/app/catalog.go"
      via: "List/search commands call catalog functions"
      pattern: "List|Search"
    - from: "internal/app/cli.go"
      to: "printResult"
      via: "Human and JSON output paths include discovery results"
      pattern: "printResult"
---

<objective>
Add provider discovery command surface (`list`/`search`) so users can discover valid keys before generation.

Purpose: Satisfy CMD-06 with deterministic, automation-friendly provider discovery.
Output: New CLI commands, catalog utility, and tests proving deterministic list/search behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-provider-selection-api-generation/2-RESEARCH.md

<interfaces>
From internal/provider/supported.go:
```go
var SupportedKeys = []string{...}
func IsSupported(key string) bool
```

From internal/app/cli.go:
```go
func Run(args []string) int
func printResult(result CommandResult, jsonOutput bool)
```

From internal/app/types.go:
```go
type CommandResult struct {
  Command string
  FinalProviders []string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic provider catalog functions</name>
  <files>internal/app/catalog.go, internal/app/catalog_test.go</files>
  <read_first>internal/provider/supported.go, internal/app/types.go</read_first>
  <behavior>
    - Test 1: `ListProviders()` returns full provider key set in alphabetical order.
    - Test 2: `SearchProviders("go")` returns deterministic filtered subset in alphabetical order.
    - Test 3: Search with no matches returns an empty list (not an error).
  </behavior>
  <action>Create `internal/app/catalog.go` with explicit exported functions for deterministic provider listing and substring search over `provider.SupportedKeys`. Implement case-insensitive search using `strings.ToLower` and always sort results before returning.</action>
  <acceptance_criteria>
    - `internal/app/catalog.go` defines list/search functions using `provider.SupportedKeys`.
    - `internal/app/catalog_test.go` asserts alphabetical ordering and no-match behavior.
    - `go test ./internal/app -run "Test(ListProviders|SearchProviders)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(ListProviders|SearchProviders)"</automated>
  </verify>
  <done>Catalog helpers provide deterministic list/search outputs and are covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Cobra list/search commands with deterministic output</name>
  <files>internal/app/cli.go, internal/app/cli_test.go</files>
  <read_first>internal/app/cli.go, internal/app/catalog.go, internal/app/json_test.go</read_first>
  <action>Add `list` and `search <term>` Cobra commands in `Run`. `list` prints all provider keys; `search` prints matching keys. Support both human output and `--json` mode by reusing structured output path (CommandResult or dedicated JSON struct), and ensure keys are sorted exactly as returned by catalog helpers.</action>
  <acceptance_criteria>
    - `internal/app/cli.go` registers `list` and `search` under root command.
    - `internal/app/cli_test.go` includes tests for successful list output and filtered search output.
    - `go test ./internal/app -run "Test(ListCommand|SearchCommand)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(ListCommand|SearchCommand)"</automated>
  </verify>
  <done>User can discover provider keys via CLI list/search with deterministic ordering in output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → search command | User-provided query term controls filtering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | Search input handling | mitigate | Treat search term as plain substring only; no regex evaluation, no shell execution, no dynamic file/system access |
| T-02-05 | R | Provider discovery output stability | mitigate | Sort list/search results and verify deterministic order via tests |
</threat_model>

<verification>
- Run `go test ./internal/app -run "Test(ListProviders|SearchProviders|ListCommand|SearchCommand)"`.
- Confirm `genignore list` and `genignore search go` produce deterministic provider ordering.
</verification>

<success_criteria>
- `list` and `search` commands exist and return expected providers.
- Discovery output is deterministic and test-backed.
- CMD-06 is fully satisfied without introducing non-deterministic provider ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-selection-api-generation/02-02-SUMMARY.md`
</output>
