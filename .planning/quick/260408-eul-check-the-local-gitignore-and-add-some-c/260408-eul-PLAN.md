---
phase: quick-260408-eul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - internal/gitignore/manager.go
  - internal/gitignore/manager_test.go
  - internal/app/service_test.go
autonomous: true
requirements:
  - quick-gitignore-cleanup
must_haves:
  truths:
    - "Generated managed blocks omit Toptal boilerplate comments while keeping real ignore rules."
    - "Running detect/add still preserves user-owned lines outside the managed markers."
    - "The repo's current .gitignore can be regenerated into the cleaned format deterministically."
  artifacts:
    - path: "internal/gitignore/manager.go"
      provides: "Managed block builder that normalizes fetched template text before writing."
    - path: "internal/gitignore/manager_test.go"
      provides: "Regression coverage for comment stripping and marker preservation."
    - path: ".gitignore"
      provides: "Real local sample showing cleaned managed block and untouched unmanaged lines."
  key_links:
    - from: "internal/app/service.go"
      to: "internal/gitignore/manager.go"
      via: "gitignore.BuildManagedBlock(finalProviders, template.Content)"
      pattern: "BuildManagedBlock\(finalProviders, template.Content\)"
---

<objective>
Clean up fetched Toptal template output so generated `.gitignore` content keeps the useful rules but drops boilerplate comments shown in the current local file.

Purpose: reduce noise/churn in the managed block without changing ownership boundaries or provider ordering.
Output: tested cleanup logic plus a regenerated local `.gitignore` sample.
</objective>

<execution_context>
@./AGENTS.md
</execution_context>

<context>
@.planning/STATE.md
@.gitignore
@internal/app/service.go
@internal/gitignore/manager.go
@internal/gitignore/manager_test.go

<interfaces>
From `internal/app/service.go`:

```go
template, err := s.Client.FetchTemplate(ctx, finalProviders)
block := gitignore.BuildManagedBlock(finalProviders, template.Content)
action, err := s.Manager.UpsertManagedBlock(block, opts.DryRun)
```

From `internal/gitignore/manager.go`:

```go
func BuildManagedBlock(providers []string, template string) string
func (m *Manager) UpsertManagedBlock(block string, dryRun bool) (FileAction, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the cleanup contract against current noisy output</name>
  <files>internal/gitignore/manager_test.go, internal/app/service_test.go</files>
  <behavior>
    - Test 1: `BuildManagedBlock` removes Toptal provenance/link/footer comments and other comment-only boilerplate from template text, while preserving section headers like `### Go ###`, actual ignore rules, negation rules, and marker metadata added by this app.
    - Test 2: detect/add flows still write a managed block with the cleaned content and leave user-owned lines outside `# BEGIN gitignore-gen` / `# END gitignore-gen` unchanged.
    - Test 3: rerunning with already-cleaned output stays byte-stable.
  </behavior>
  <action>Use the current local `.gitignore` as the reference for what must disappear: strip the extra Toptal-generated comment noise inside the managed region, but do not remove the app-owned header lines (`Generated by`, `Providers`) or any real ignore entries. Keep this scoped to regression tests first so the cleanup rules are explicit before implementation.</action>
  <verify>
    <automated>go test ./internal/gitignore ./internal/app</automated>
  </verify>
  <done>Failing tests precisely describe which comment lines are removed, which structural lines remain, and that unmanaged file content is still preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement template normalization and refresh the local sample</name>
  <files>internal/gitignore/manager.go, .gitignore</files>
  <action>Add a small normalization step in the managed-block path so all commands that consume `template.Content` write the cleaned version automatically. Keep provider ordering and marker ownership unchanged, avoid touching code outside the block-writing path, and regenerate the repo's `.gitignore` so the checked-in sample reflects the new output while preserving `.planning` and any other unmanaged lines outside the markers.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <done>The repo `.gitignore` managed block no longer contains the unneeded Toptal comments, tests pass, and reruns remain deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toptal API → template normalization | Untrusted remote text is converted into managed local file content. |
| Managed block writer → user `.gitignore` | App-owned generated text is merged into a user-edited file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | `internal/gitignore/manager.go` | mitigate | Normalize only comment-only boilerplate lines from fetched template content; preserve real ignore patterns and app-owned metadata via tests before implementation. |
| T-quick-02 | I | `.gitignore` merge path | mitigate | Keep cleanup inside `BuildManagedBlock` and continue using marker-bounded `UpsertManagedBlock` so lines outside the managed region remain untouched. |
| T-quick-03 | R | deterministic rewrites | mitigate | Add idempotence assertions so cleaned output is byte-stable across reruns and does not churn the file unnecessarily. |
</threat_model>

<verification>
- `go test ./internal/gitignore ./internal/app` for focused cleanup regressions
- `go test ./...` for repo-wide safety after wiring and refreshing `.gitignore`
</verification>

<success_criteria>
- Managed block generation removes the noisy Toptal comments seen in the current local `.gitignore`.
- Section headers and actual ignore patterns remain intact.
- User-owned content outside the managed markers is unchanged.
- Full test suite passes after the local `.gitignore` sample is refreshed.
</success_criteria>

<output>
After completion, summarize the cleanup rules applied and note that the local `.gitignore` sample was regenerated.
</output>
