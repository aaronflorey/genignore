---
quick: 260411-47x
type: execute
autonomous: true
files_modified:
  - internal/gitignore/manager.go
  - internal/gitignore/manager_test.go
must_haves:
  truths:
    - "When generated rules duplicate existing user rules outside managed markers, duplicates are removed from outside content before write."
    - "Only non-comment managed-block rules participate in dedupe comparison."
    - "Equivalent forms are deduped only when semantics are unchanged (e.g., /.DS_Store vs .DS_Store; storage/ vs storage only when safe)."
  artifacts:
    - path: "internal/gitignore/manager.go"
      provides: "Safe dedupe pass between managed and unmanaged lines during merge"
    - path: "internal/gitignore/manager_test.go"
      provides: "Regression coverage for dedupe and no-semantic-change safeguards"
  key_links:
    - from: "BuildManagedBlock output"
      to: "mergeManagedBlock"
      via: "pre-write dedupe of unmanaged content"
      pattern: "managed non-comment rule set drives unmanaged-line filtering"
---

<objective>
Add a safe pre-write deduplication step so rules generated inside `# BEGIN genignore` / `# END genignore` are not redundantly kept outside the managed block.

Purpose: Keep `.gitignore` deterministic and clean without deleting meaningful user intent.
Output: Updated merge/dedupe logic plus targeted tests proving exact behavior.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@internal/gitignore/manager.go
@internal/gitignore/manager_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for managed-vs-unmanaged dedupe rules</name>
  <files>internal/gitignore/manager_test.go</files>
  <behavior>
    - Test 1: If managed block contains `.DS_Store` and unmanaged content contains `.DS_Store`, unmanaged duplicate is removed.
    - Test 2: Equivalent safe forms dedupe (`/.DS_Store` vs `.DS_Store`).
    - Test 3: Comments and blank lines outside block are preserved and never used as dedupe candidates.
    - Test 4: Potential semantic changes are preserved (do not dedupe when equivalence is ambiguous/not safe).
  </behavior>
  <action>Write focused failing tests around `mergeManagedBlock`/`UpsertManagedBlock` for exact dedupe behavior from this quick request. Cover both exact match and narrowly-approved equivalence handling, and include at least one explicit non-dedupe guard case for safety.</action>
  <verify>
    <automated>go test ./internal/gitignore -run "TestUpsert.*Dedup|TestMerge.*Dedup"</automated>
  </verify>
  <done>Tests fail first against current behavior and precisely encode requested dedupe semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement safe unmanaged-line dedupe before final write</name>
  <files>internal/gitignore/manager.go</files>
  <behavior>
    - Dedupes unmanaged non-comment rules that are already present as non-comment managed rules.
    - Uses limited canonicalization only where behavior is unchanged.
    - Keeps ordering of remaining unmanaged lines intact.
  </behavior>
  <action>Add a dedupe pass in merge flow (before final content assembly) that: (1) extracts managed rule candidates excluding comments/blank lines, (2) compares unmanaged lines outside markers using exact and narrowly-safe normalized forms, (3) removes only confirmed duplicates, and (4) preserves all non-duplicates byte-exact where practical. Keep implementation local to `internal/gitignore/manager.go` (no new dependency).</action>
  <verify>
    <automated>go test ./internal/gitignore -run "TestUpsert.*Dedup|TestMerge.*Dedup|TestUpsertReplacesOnlyManagedRegion|TestUpsertIsIdempotentForEquivalentRun"</automated>
  </verify>
  <done>All new dedupe tests pass, existing marker-safety tests remain green, and idempotent reruns stay stable.</done>
</task>

<task type="auto">
  <name>Task 3: Run targeted package verification for regression safety</name>
  <files>internal/gitignore/manager.go, internal/gitignore/manager_test.go</files>
  <action>Run the full `internal/gitignore` test package to confirm no regressions beyond the new behavior and ensure deterministic output expectations still hold.</action>
  <verify>
    <automated>go test ./internal/gitignore</automated>
  </verify>
  <done>`internal/gitignore` package passes fully with new dedupe behavior and no unrelated behavior drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI template input -> `.gitignore` write path | Generated content can alter repository ignore behavior if dedupe is overly aggressive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260411-01 | T (Tampering) | `mergeManagedBlock` dedupe logic | mitigate | Restrict dedupe to non-comment managed rules and explicitly tested safe canonical forms only |
| T-260411-02 | D (Denial of service via behavior break) | `.gitignore` semantics | mitigate | Add guard tests ensuring ambiguous equivalence is preserved and not removed |
| T-260411-03 | R (Repudiation) | Rule ownership clarity | accept | Managed markers continue to identify CLI-owned region; outside lines remain user-owned except exact/safe duplicates |
</threat_model>

<verification>
- `go test ./internal/gitignore -run "TestUpsert.*Dedup|TestMerge.*Dedup"`
- `go test ./internal/gitignore`
</verification>

<success_criteria>
- Duplicate unmanaged rule lines that are already represented inside managed block are removed safely.
- Only non-comment managed lines are dedupe keys.
- Safe equivalence examples are covered and enforced by tests.
- Existing managed-marker safety and idempotency guarantees remain intact.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-47x-before-writing-the-generated-gitignore-c/260411-47x-SUMMARY.md`.
</output>
