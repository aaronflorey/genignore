---
phase: quick-260411-989
plan: 01
subsystem: ci
tags: [github-actions, ci, lint, test, go]
requires: []
provides:
  - push and pull_request CI workflow that runs golangci-lint and go test with Go version from go.mod
  - contributor-facing local verification commands aligned with CI checks
affects: [.github/workflows/ci.yml, README.md, internal/api/client.go]
tech-stack:
  added: []
  patterns: [least-privilege GitHub Actions permissions, deterministic Go setup via go-version-file]
key-files:
  created: [.github/workflows/ci.yml]
  modified: [README.md, internal/api/client.go]
key-decisions:
  - "Use a single lint-and-test job for fast failure on push/PR checks."
  - "Keep CI permissions minimal (`contents: read`) because workflow has no write/secrets requirements."
requirements-completed: [quick-ci-test-lint-on-push-pr]
duration: 10min
completed: 2026-04-11
---

# Phase quick-260411-989 Plan 01: Please setup a workflow for testing the Summary

**Added a least-privilege GitHub Actions CI workflow that runs lint and tests on push/PR using Go from `go.mod`, and documented matching local commands in README.**

## Performance

- **Duration:** 10 min
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `.github/workflows/ci.yml` with `push` + `pull_request` triggers, `actions/setup-go` using `go-version-file: go.mod`, `golangci/golangci-lint-action`, and `go test ./...`.
- Added a concise `Development checks` section in `README.md` with `go test ./...` and `golangci-lint run`.
- Fixed pre-existing `errcheck` failures in `internal/api/client.go` (unchecked `res.Body.Close()`), so lint now passes for the new CI gate.

## Task Commits

1. **Task 1: Add push/PR CI workflow for lint and tests** - `ddb1ad1` (ci)
2. **Task 2: Document local commands that mirror CI checks** - `8cd813b` (fix)

**Plan metadata:** not committed per quick-task constraints.

## Files Created/Modified

- `.github/workflows/ci.yml` - New CI workflow for lint + test on every push and pull request.
- `README.md` - Added local development checks that mirror CI.
- `internal/api/client.go` - Added explicit ignored close-return handling to satisfy lint gate.

## Decisions Made

- Keep CI separate from release automation by creating a dedicated `ci.yml` workflow.
- Apply minimal workflow permissions (`contents: read`) to mitigate untrusted branch code execution risk.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking Issue] Local lint tool missing from PATH**
- **Found during:** Task 2 verification
- **Issue:** `golangci-lint run` could not execute because binary was not available via `rtk` PATH.
- **Fix:** Installed `golangci-lint` locally and ran verification with absolute binary path.

**2. [Rule 3 - Blocking Issue] Pre-existing lint failures blocked CI-parity verification**
- **Found during:** Task 2 verification
- **Issue:** `internal/api/client.go` had unchecked `res.Body.Close()` errors flagged by `errcheck`.
- **Fix:** Updated both deferred closes to explicitly ignore returned error via `_ = res.Body.Close()`.
- **Files modified:** `internal/api/client.go`
- **Commit:** `8cd813b`

## Issues Encountered

- Local `rtk golangci-lint` command did not resolve the newly installed binary path; used `$(go env GOPATH)/bin/golangci-lint` for verification.

## User Setup Required

- None.

## Self-Check: PASSED

- Found `.planning/quick/260411-989-please-setup-a-workflow-for-testing-the-/260411-989-SUMMARY.md`.
- Found task commits `ddb1ad1` and `8cd813b` in `git log --oneline --all`.
