---
quick: 260411-989
type: execute
autonomous: true
files_modified:
  - .github/workflows/ci.yml
  - README.md
requirements:
  - quick-ci-test-lint-on-push-pr
must_haves:
  truths:
    - "Every pushed commit runs automated Go tests in GitHub Actions."
    - "Every pull request runs lint and test checks before merge."
    - "CI uses deterministic Go version/setup from repository config."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow triggered on push and pull_request with lint + test jobs"
    - path: "README.md"
      provides: "Contributor-visible CI behavior and matching local verification commands"
  key_links:
    - from: "GitHub event triggers (push, pull_request)"
      to: "lint/test jobs"
      via: "workflow on: configuration"
      pattern: "on: push + pull_request"
    - from: "actions/setup-go"
      to: "go test ./..."
      via: "job steps"
      pattern: "setup-go with go-version-file: go.mod"
---

<objective>
Add a CI workflow that enforces linting and test execution for each commit (push) and pull request.

Purpose: prevent regressions and style drift before code lands.
Output: one GitHub Actions workflow for lint + tests plus short README guidance for local parity.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@go.mod
@README.md
@.github/workflows/release-please.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add push/PR CI workflow for lint and tests</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create a new workflow triggered on `push` and `pull_request` that runs on `ubuntu-latest`, checks out code, sets up Go using `go-version-file: go.mod`, restores module/build cache, runs `go test ./...`, and runs golangci-lint via the official `golangci/golangci-lint-action`. Keep it separate from release automation and fail fast on lint/test errors.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <done>The repository has a CI workflow that automatically runs lint + tests on every push and pull request.</done>
</task>

<task type="auto">
  <name>Task 2: Document local commands that mirror CI checks</name>
  <files>README.md</files>
  <action>Add a concise "Development checks" section that shows contributors how to run the same validation locally (`go test ./...` and `golangci-lint run`) before pushing. Keep wording short and aligned with the new CI workflow.</action>
  <verify>
    <automated>go test ./... && golangci-lint run</automated>
  </verify>
  <done>README clearly states the exact local commands that correspond to CI lint/test gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor branch -> GitHub Actions runner | Untrusted code executes in CI context and must remain least-privilege. |
| PR status checks -> merge decision | Check outcomes gate integration into protected branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260411-989-01 | T | `.github/workflows/ci.yml` | mitigate | Keep workflow scoped to lint/test only with minimal permissions and no secret-dependent steps. |
| T-260411-989-02 | D | CI pipeline availability | accept | Standard GitHub-hosted runner reliability accepted for this repo; failures surface quickly via required checks. |
| T-260411-989-03 | R | Contributor validation claims | mitigate | Require deterministic automated checks (`go test ./...`, `golangci-lint`) on push/PR so status is auditable. |
</threat_model>

<verification>
- `go test ./...`
- `golangci-lint run`
</verification>

<success_criteria>
- New commits pushed to GitHub automatically trigger lint and test workflow runs.
- Pull requests automatically run the same lint and test checks.
- CI setup reads Go version from `go.mod` and remains independent from release workflow.
- README includes the local verification commands matching CI.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-989-please-setup-a-workflow-for-testing-the-/260411-989-SUMMARY.md`.
</output>
