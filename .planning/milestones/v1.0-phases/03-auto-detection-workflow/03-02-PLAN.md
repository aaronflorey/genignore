---
phase: 03-auto-detection-workflow
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - internal/app/cli.go
  - internal/app/cli_test.go
  - internal/app/json_test.go
  - internal/app/types.go
autonomous: true
requirements: [DET-02]
must_haves:
  truths:
    - "User can inspect detection evidence in verbose terminal output without cluttering default output"
    - "User can inspect detection evidence in JSON output using a stable structured payload"
    - "Default detect output remains concise and deterministic"
  artifacts:
    - path: "internal/app/cli.go"
      provides: "Verbose/default detect rendering and JSON output path"
    - path: "internal/app/cli_test.go"
      provides: "CLI behavior coverage for detect verbose and default modes"
    - path: "internal/app/json_test.go"
      provides: "Structured JSON assertions for detection evidence fields"
  key_links:
    - from: "internal/app/cli.go"
      to: "internal/app/service.go"
      via: "Detect command renders service CommandResult in human and JSON modes"
      pattern: "service\.Detect|printResult"
    - from: "internal/app/types.go"
      to: "internal/app/json_test.go"
      via: "CommandResult JSON shape must include detection results predictably"
      pattern: "DetectionResults"
---

<objective>
Finish the user-visible evidence path for Phase 3 so detection rationale is inspectable in verbose and JSON modes while default output stays concise.

Purpose: Satisfy DET-02 without dragging all of Phase 4 output-contract work forward.
Output: Updated CLI rendering, stable JSON assertions, and tests proving evidence visibility boundaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-auto-detection-workflow/03-CONTEXT.md

<interfaces>
From internal/app/cli.go:
```go
func Run(args []string) int
func printResult(result CommandResult, jsonOutput bool)
```

From internal/app/types.go:
```go
type CommandResult struct {
  DetectionResults []provider.Result `json:"detectionResults,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI coverage for default vs verbose detect evidence output</name>
  <files>internal/app/cli_test.go</files>
  <read_first>internal/app/cli.go, internal/app/cli_test.go, internal/app/service.go</read_first>
  <behavior>
    - Test 1: Default detect output shows final selection summary but omits detector-by-detector evidence.
    - Test 2: `detect --verbose` renders matched/error evidence entries with provider, reason, and evidence/path when available.
    - Test 3: Detect failures still write errors to stderr and return non-zero exit behavior.
  </behavior>
  <action>Expand CLI tests around `detect` using a lightweight injection seam or equivalent test hook so tests do not depend on live API calls. Assert concise default output boundaries and explicit verbose evidence lines.</action>
  <acceptance_criteria>
    - `internal/app/cli_test.go` includes detect coverage for both default and verbose modes.
    - Tests do not rely on the live Toptal API.
    - `go test ./internal/app -run "TestDetect.*Command"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "TestDetect.*Command"</automated>
  </verify>
  <done>CLI output boundaries for Phase 3 evidence are explicit and regression-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock JSON evidence shape for detect results</name>
  <files>internal/app/json_test.go, internal/app/types.go</files>
  <read_first>internal/app/types.go, internal/app/json_test.go</read_first>
  <behavior>
    - Test 1: JSON detect payload includes `detectionResults` when evidence is present.
    - Test 2: Each evidence entry preserves `key`, `matched`, `reason`, `evidence`, and `error` fields predictably.
    - Test 3: JSON output remains machine-readable with no human labels mixed into stdout payloads.
  </behavior>
  <action>Broaden JSON tests so `CommandResult` evidence payloads are asserted structurally rather than by loose substring checks alone. Keep the schema minimal and aligned with the existing `provider.Result` contract.</action>
  <acceptance_criteria>
    - `internal/app/json_test.go` verifies `detectionResults` structure directly.
    - No new detect-only JSON type is introduced unless strictly necessary.
    - `go test ./internal/app -run "TestJSON"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "TestJSON"</automated>
  </verify>
  <done>JSON evidence output is stable enough for Phase 3 automation needs and explicitly tested.</done>
</task>

<task type="auto">
  <name>Task 3: Refine human-readable detect rendering for concise default output and useful verbose evidence</name>
  <files>internal/app/cli.go</files>
  <read_first>internal/app/cli.go, internal/app/cli_test.go, internal/app/json_test.go</read_first>
  <action>Update `printResult` and any required CLI seams so default detect output stays summary-first, while verbose mode renders deterministic detection evidence entries only when requested. Keep JSON output on the existing structured path and avoid pulling broader Phase 4 formatting work into this plan.</action>
  <acceptance_criteria>
    - `internal/app/cli.go` differentiates concise default output from verbose evidence output.
    - Human-readable evidence order matches the sorted `DetectionResults` order from the service layer.
    - JSON output remains a plain structured payload with no prose labels.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app</automated>
  </verify>
  <done>Users can inspect why providers were detected without cluttering normal command output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service result → terminal output | Structured evidence is rendered for humans without breaking default readability |
| Service result → JSON output | Structured evidence becomes automation-visible data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | Verbose detect output | mitigate | Keep detailed evidence behind `--verbose`; omit unmatched detector noise from default output |
| T-03-05 | R | JSON evidence stability | mitigate | Reuse `provider.Result` and add direct JSON tests for evidence fields |
| T-03-06 | D | CLI detect testing | mitigate | Add a test seam or equivalent injection point so detect CLI tests stay offline and deterministic |
</threat_model>

<verification>
- Run `go test ./internal/app`.
- Confirm DET-02 is covered in both verbose human-readable and JSON output paths.
</verification>

<success_criteria>
- Verbose detect output exposes useful evidence without changing default concise output.
- JSON detect output includes stable structured evidence entries.
- CLI tests stay deterministic and do not depend on the live API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auto-detection-workflow/03-02-SUMMARY.md`
</output>
