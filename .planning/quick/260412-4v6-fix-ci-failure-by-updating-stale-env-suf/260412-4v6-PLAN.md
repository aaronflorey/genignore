---
quick: 260412-4v6
type: execute
autonomous: true
files_modified:
  - internal/app/service_test.go
must_haves:
  truths:
    - "CI no longer fails on stale env suffix expectations in app service tests."
    - "Test expectations match current env-rule normalization and dedupe behavior."
    - "Full repository test suite passes, including go test ./..."
  artifacts:
    - path: "internal/app/service_test.go"
      provides: "Updated assertions/fixtures reflecting normalized + deduped env behavior"
  key_links:
    - from: "Service test expected output"
      to: "current env normalization behavior"
      via: "assertion updates in stale env suffix checks"
      pattern: "expected values exactly match dedupe+normalization outputs"
---

<objective>
Fix the CI regression caused by stale env suffix assertions in `internal/app/service_test.go` so tests validate the current normalization and dedupe contract instead of pre-change behavior.

Purpose: restore green CI while preserving the new env-rule correctness guarantees.
Output: updated service test assertions and passing package + full-suite verification.
</objective>

<context>
@.planning/STATE.md
@AGENTS.md
@internal/app/service.go
@internal/app/service_test.go

<interfaces>
From `internal/app/service.go`:

```go
func (s *Service) Detect(ctx context.Context, opts DetectOptions) (CommandResult, error)
func (s *Service) Add(ctx context.Context, opts AddOptions) (CommandResult, error)
```

From `internal/app/service_test.go`:

```go
type fakeAPI struct { /* available/template stubs used by service tests */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale env suffix assertion to match normalized/deduped behavior</name>
  <files>internal/app/service_test.go</files>
  <action>Locate the failing env-suffix assertion(s) in service tests and update the expected value(s) so they match the current env-rule normalization + dedupe output contract. Keep the test intent identical (validate behavior, not implementation details), and do not loosen assertions to generic contains-only checks.</action>
  <verify>
    <automated>go test ./internal/app -run TestService -count=1</automated>
  </verify>
  <done>The previously stale assertion now asserts the post-normalization deduped value exactly and targeted service tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Align related expected fixtures in the same test block for deterministic ordering</name>
  <files>internal/app/service_test.go</files>
  <action>Adjust any adjacent expected slices/strings in the same scenario so ordering and dedupe assumptions are fully consistent with current deterministic behavior. Keep coverage focused on env-rule suffix normalization/dedupe regression and avoid unrelated test rewrites.</action>
  <verify>
    <automated>go test ./internal/app -count=1</automated>
  </verify>
  <done>Service package tests are stable with deterministic expected outputs and no flaky ordering assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Run full regression verification including CI parity command</name>
  <files>internal/app/service_test.go</files>
  <action>Run full repository tests to confirm the fix resolves CI without introducing regressions outside app service tests.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <done>`go test ./...` succeeds end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test expectations -> CI quality gate | Incorrect expectations can mask regressions or produce false failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260412-4v6-01 | T (Tampering) | `internal/app/service_test.go` assertions | mitigate | Keep strict expected-value assertions aligned to normalization+dedupe contract (no weakened checks) |
| T-260412-4v6-02 | D (Denial of service via failing CI) | CI test stage (`go test ./...`) | mitigate | Validate with targeted app tests then full `go test ./...` to confirm pipeline parity |
</threat_model>

<verification>
- `go test ./internal/app -run TestService -count=1`
- `go test ./internal/app -count=1`
- `go test ./...`
</verification>

<success_criteria>
- Stale env suffix assertion(s) in `internal/app/service_test.go` reflect current normalization + dedupe behavior.
- `internal/app` tests pass consistently.
- `go test ./...` passes and CI parity is restored.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-4v6-fix-ci-failure-by-updating-stale-env-suf/260412-4v6-SUMMARY.md`.
</output>
