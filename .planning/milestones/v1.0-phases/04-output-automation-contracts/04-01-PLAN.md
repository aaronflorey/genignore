---
phase: 04-output-automation-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/app/types.go
  - internal/app/json_test.go
autonomous: true
requirements: [OUT-02, OUT-03]
must_haves:
  truths:
    - "User can run detect or add with --json and receive a stable machine-readable payload"
    - "User can run detect or add with --dry-run and see the same command result contract without writing .gitignore"
    - "Warnings, selections, detection evidence, and file action remain deterministically ordered in JSON output"
  artifacts:
    - path: "internal/app/types.go"
      provides: "Stable shared command payload shape for detect/add automation output"
    - path: "internal/app/service.go"
      provides: "Dry-run execution path and deterministic result population for detect/add"
    - path: "internal/app/json_test.go"
      provides: "Contract-focused JSON assertions for detect/add payloads"
  key_links:
    - from: "internal/app/service.go"
      to: "internal/gitignore/manager.go"
      via: "Service must preserve live command behavior while passing dry-run through to managed-block writes"
      pattern: "UpsertManagedBlock|DryRun"
    - from: "internal/app/service.go"
      to: "internal/app/types.go"
      via: "Service populates the stable JSON result contract consumed by the CLI"
      pattern: "CommandResult|FileAction|DetectionResults"
---

<objective>
Lock the automation-facing contract for `detect` and `add` so scripts can rely on stable JSON fields and write-free dry-run behavior.

Purpose: Satisfy the machine-readable and dry-run half of Phase 4 before refining the terminal UX.
Output: Updated result types/service behavior plus tests proving stable `--json` and `--dry-run` semantics for command execution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-automation-contracts/04-CONTEXT.md

<interfaces>
From internal/app/types.go:
```go
type CommandResult struct {
  Command string
  CWD string
  DetectedProviders []string
  IncludedProviders []string
  ExcludedProviders []string
  AddedProviders []string
  FinalProviders []string
  UnsupportedKeyWarnings []string
  RemoteProviderWarnings []string
  DetectionResults []provider.Result
  FileAction gitignore.FileAction
  TemplateProviderCount int
}
```

From internal/app/service.go:
```go
func (s *Service) Detect(ctx context.Context, opts DetectOptions) (CommandResult, error)
func (s *Service) Add(ctx context.Context, opts AddOptions) (CommandResult, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract tests for detect/add JSON payloads and dry-run behavior</name>
  <files>internal/app/json_test.go, internal/app/service_test.go</files>
  <read_first>internal/app/types.go, internal/app/service.go, internal/app/json_test.go, internal/app/service_test.go</read_first>
  <behavior>
    - Test 1: `detect --json` includes stable command metadata, selection slices, warning slices, detection results, file action, and template count.
    - Test 2: `add --json` includes the shared top-level fields, omitting detect-only fields only when empty.
    - Test 3: `--dry-run` returns success and a dry-run file action while making no persistent `.gitignore` mutation.
    - Test 4: Warnings and provider arrays are asserted in deterministic alphabetical order.
  </behavior>
  <action>Expand JSON and service tests so they decode payloads structurally and verify dry-run semantics through the real service path. Assert exact field names, exact ordering, and exact file-action expectations rather than loose substring checks.</action>
  <acceptance_criteria>
    - `internal/app/json_test.go` verifies detect and add payloads structurally.
    - `internal/app/service_test.go` proves dry-run leaves managed file content unchanged while still returning a real file action.
    - `go test ./internal/app -run "Test(JSON|Detect|Add)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(JSON|Detect|Add)"</automated>
  </verify>
  <done>Automation contract expectations are explicit before implementation updates are finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten shared result types for stable command JSON output</name>
  <files>internal/app/types.go, internal/app/json_test.go</files>
  <read_first>internal/app/types.go, internal/app/json_test.go, internal/app/cli.go</read_first>
  <action>Refine `CommandResult` only as needed to keep top-level JSON metadata stable across detect and add. Prefer minimal additive changes over new per-command payload types, and preserve camelCase naming plus omitempty behavior where it helps avoid noisy empty fields.</action>
  <acceptance_criteria>
    - `internal/app/types.go` remains the single shared detect/add payload definition.
    - New fields are introduced only when required by OUT-02 or OUT-03.
    - JSON output stays deterministic under `encoding/json` without map-based assembly.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "TestJSON"</automated>
  </verify>
  <done>The detect/add JSON contract is centralized and intentionally shaped rather than incidental.</done>
</task>

<task type="auto">
  <name>Task 3: Align service dry-run behavior with the final automation contract</name>
  <files>internal/app/service.go, internal/app/service_test.go</files>
  <read_first>internal/app/service.go, internal/gitignore/manager.go, internal/app/service_test.go</read_first>
  <action>Ensure `Detect` and `Add` execute the full selection, warning, template-fetch, and managed-block action path for dry-runs, differing only at the write boundary. Keep API failure and empty-detect hard-failure behavior unchanged, and make sure returned `FileAction` values distinguish dry-run from live writes clearly enough for automation.</action>
  <acceptance_criteria>
    - Dry-run never writes `.gitignore` content.
    - Dry-run still returns the same ordered providers, warnings, and template count as a live success path.
    - Error paths remain non-successful and do not emit partial success results.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app</automated>
  </verify>
  <done>Detect/add service behavior is stable for both local scripts and CI dry-run checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service result → JSON stdout | Automation depends on field presence, ordering, and naming stability |
| Dry-run execution → file system | Real command logic runs against local state but must stop at the write boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | R | JSON command contract | mitigate | Keep a single typed payload, avoid map assembly, and assert exact JSON fields in tests |
| T-04-02 | T | Dry-run file safety | mitigate | Reuse live command flow but stop before write; add tests that confirm no file mutation |
| T-04-03 | D | Automation-facing ordering drift | mitigate | Preserve sorted slices in service results and assert exact ordering in JSON/service tests |
</threat_model>

<verification>
- Run `go test ./internal/app`.
- Confirm OUT-02 and OUT-03 are covered for both `detect` and `add`.
</verification>

<success_criteria>
- `detect --json` and `add --json` expose a stable shared payload.
- `--dry-run` behaves like a write-free success path, not a fake alternate mode.
- Ordered selections, warnings, and evidence remain deterministic in service results and JSON output.
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-automation-contracts/04-01-SUMMARY.md`
</output>
