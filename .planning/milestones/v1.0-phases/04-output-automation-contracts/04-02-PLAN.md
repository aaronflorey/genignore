---
phase: 04-output-automation-contracts
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - internal/app/cli.go
  - internal/app/cli_test.go
  - internal/app/json_test.go
autonomous: true
requirements: [OUT-01, OUT-02]
must_haves:
  truths:
    - "User gets concise labeled terminal output for detect, add, list, and search"
    - "Warnings and file action are visible in human-readable output without verbose prose"
    - "List/search human and JSON outputs follow a stable automation-friendly contract aligned with other commands"
  artifacts:
    - path: "internal/app/cli.go"
      provides: "Human-readable summary rendering and JSON output for command and catalog flows"
    - path: "internal/app/cli_test.go"
      provides: "CLI-level contract coverage for labeled output, warnings, and dry-run/file-action summaries"
    - path: "internal/app/json_test.go"
      provides: "Catalog JSON assertions for list/search stability"
  key_links:
    - from: "internal/app/cli.go"
      to: "internal/app/types.go"
      via: "CLI renders the stable command payload from the service layer"
      pattern: "printResult|CommandResult"
    - from: "internal/app/cli.go"
      to: "internal/app/catalog.go"
      via: "List/search use deterministic provider catalog results and expose them in human and JSON modes"
      pattern: "printCatalogResult|ListProviders|SearchProviders"
---

<objective>
Finish Phase 4 by making the terminal output compact, predictable, and aligned with the automation contracts already locked for JSON and dry-run behavior.

Purpose: Satisfy the user-facing readability contract without introducing a second, drifting output model.
Output: Refined CLI renderers and contract tests covering human-readable and JSON output for detect, add, list, and search.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-output-automation-contracts/04-CONTEXT.md

<interfaces>
From internal/app/cli.go:
```go
func Run(args []string) int
func printResult(result CommandResult, jsonOutput bool, verbose bool)
func printCatalogResult(result CatalogResult, jsonOutput bool)
```

From internal/app/cli.go:
```go
type CatalogResult struct {
  Command string
  Query string
  Providers []string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI contract tests for concise human-readable summaries</name>
  <files>internal/app/cli_test.go</files>
  <read_first>internal/app/cli.go, internal/app/cli_test.go, internal/app/service.go</read_first>
  <behavior>
    - Test 1: Default `detect` output shows command, populated selection summaries, warnings, and file action, without verbose detector details.
    - Test 2: Default `add` output shows added/final providers, warnings, and file action.
    - Test 3: `list` and `search` outputs show command/query plus one provider per line in deterministic order.
    - Test 4: Dry-run output clearly indicates a dry-run file action in human-readable mode.
  </behavior>
  <action>Expand CLI tests to assert exact labeled lines or stable output fragments for default command output. Keep tests offline by continuing to inject fake services where needed, and verify that only populated sections appear in successful summaries.</action>
  <acceptance_criteria>
    - `internal/app/cli_test.go` covers detect, add, list, and search default output.
    - Tests assert warnings and file action visibility explicitly.
    - `go test ./internal/app -run "Test(Detect|Add|List|Search).*Command"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(Detect|Add|List|Search).*Command"</automated>
  </verify>
  <done>Human-readable output boundaries are defined by tests rather than ad hoc expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JSON contract coverage for catalog commands</name>
  <files>internal/app/json_test.go</files>
  <read_first>internal/app/cli.go, internal/app/json_test.go, internal/app/catalog.go</read_first>
  <behavior>
    - Test 1: `list --json` returns `command` plus sorted `providers`.
    - Test 2: `search --json` returns `command`, `query`, and sorted `providers`.
    - Test 3: Catalog JSON output contains no human-readable labels mixed into stdout.
  </behavior>
  <action>Add structural JSON assertions for `CatalogResult` output so list/search become stable automation surfaces as well. Reuse existing JSON test patterns and avoid introducing alternative schemas without need.</action>
  <acceptance_criteria>
    - `internal/app/json_test.go` includes explicit list/search payload assertions.
    - Sorted provider ordering is asserted directly for catalog commands.
    - `go test ./internal/app -run "TestJSON"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "TestJSON"</automated>
  </verify>
  <done>All user-visible commands have a tested JSON output contract.</done>
</task>

<task type="auto">
  <name>Task 3: Refine CLI renderers to keep output compact and contract-stable</name>
  <files>internal/app/cli.go</files>
  <read_first>internal/app/cli.go, internal/app/cli_test.go, internal/app/json_test.go</read_first>
  <action>Update `printResult` and `printCatalogResult` so default terminal output is summary-first, deterministic, and limited to populated sections. Preserve verbose detection evidence behind `--verbose`, keep warning ordering stable, and ensure list/search human output stays aligned with JSON metadata fields rather than drifting into a second command-specific format.</action>
  <acceptance_criteria>
    - Default output for detect/add stays concise and labeled.
    - Verbose detection evidence remains available only when requested.
    - List/search output remains deterministic in both human and JSON modes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app</automated>
  </verify>
  <done>Phase 4 output contracts are fully represented in the CLI layer for both people and automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service/catalog results → terminal output | Stable data is rendered for humans without becoming noisy or ambiguous |
| Catalog data → JSON stdout | Discovery commands become automation inputs and must remain schema-stable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | Default terminal summaries | mitigate | Show only populated sections and keep verbose evidence opt-in |
| T-04-05 | R | Human/JSON output drift | mitigate | Derive both paths from typed structs and lock behavior with CLI + JSON tests |
| T-04-06 | D | Catalog output ordering | mitigate | Reuse sorted provider catalog results and assert exact order in tests |
</threat_model>

<verification>
- Run `go test ./internal/app`.
- Confirm OUT-01 and OUT-02 are covered across `detect`, `add`, `list`, and `search`.
</verification>

<success_criteria>
- Default output is concise, labeled, and stable for all commands.
- Catalog commands have stable JSON payloads in addition to stable terminal output.
- Verbose evidence remains available without cluttering normal command runs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-automation-contracts/04-02-SUMMARY.md`
</output>
