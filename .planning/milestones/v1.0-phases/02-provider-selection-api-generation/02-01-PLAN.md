---
phase: 02-provider-selection-api-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/api/client.go
  - internal/api/client_test.go
autonomous: true
requirements: [CMD-05, DET-03, DET-04, API-01, API-02, API-03]
must_haves:
  truths:
    - "User can run add with mixed valid/invalid keys and valid providers are still applied"
    - "User sees unsupported key warnings and remote-provider-drift warnings"
    - "Provider ordering remains alphabetical in API requests and command results"
    - "API failure causes command failure instead of partial success"
  artifacts:
    - path: "internal/app/service.go"
      provides: "Add/detect provider sanitization, warning generation, deterministic selection"
    - path: "internal/api/client.go"
      provides: "Toptal provider-list and template API client behavior"
    - path: "internal/app/service_test.go"
      provides: "Behavioral regression tests for add/detect semantics"
  key_links:
    - from: "internal/app/service.go"
      to: "internal/api/client.go"
      via: "AvailableProviders + FetchTemplate calls before block write"
      pattern: "Client\\.(AvailableProviders|FetchTemplate)"
    - from: "internal/app/service.go"
      to: "internal/gitignore/manager.go"
      via: "BuildManagedBlock and UpsertManagedBlock"
      pattern: "UpsertManagedBlock"
---

<objective>
Harden deterministic provider/API behavior for `detect` and `add` so Phase 2 command semantics remain stable under invalid input, remote drift, and API failures.

Purpose: Close reliability gaps around requirement-critical behavior already present in core flows.
Output: Updated service/API code and regression tests proving deterministic ordering + warning/failure contracts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-provider-selection-api-generation/2-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand deterministic provider/API regression coverage</name>
  <files>internal/app/service_test.go, internal/api/client_test.go</files>
  <read_first>internal/app/service.go, internal/api/client.go, internal/app/service_test.go, internal/api/client_test.go</read_first>
  <behavior>
    - Test 1: Add path keeps valid keys while returning unsupported warnings when mixed input is provided.
    - Test 2: Final provider set and template request provider list are alphabetically sorted.
    - Test 3: Remote-provider-drift warning appears when supported local key is absent from remote list.
    - Test 4: API list/template failures return errors (hard-fail) and do not perform write-side success flow.
  </behavior>
  <action>Add table-style tests covering CMD-05, DET-03, DET-04, API-01, API-02, API-03 contracts. Assert exact provider slices and warning prefixes (`unsupported provider key:`, `supported provider missing remotely:`), and assert template request providers are sorted.</action>
  <acceptance_criteria>
    - `internal/app/service_test.go` contains `TestAdd` and/or `TestDetect` cases asserting sorted provider arrays and warning strings.
    - `internal/api/client_test.go` contains an assertion that provider-list results are deterministic.
    - `go test ./internal/app ./internal/api` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app ./internal/api</automated>
  </verify>
  <done>Regression tests fail before implementation change (if needed) and pass after updates, proving deterministic ordering and hard-fail behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce deterministic provider-list ordering in API + service flow</name>
  <files>internal/api/client.go, internal/app/service.go</files>
  <read_first>internal/api/client.go, internal/app/service.go, internal/app/service_test.go, internal/api/client_test.go</read_first>
  <action>Update provider-list handling to return a sorted provider slice from `AvailableProviders`, and ensure service warning generation/selection logic consumes deterministic slices only. Keep existing hard-failure behavior on non-2xx or request errors unchanged (API-02).</action>
  <acceptance_criteria>
    - `internal/api/client.go` includes deterministic sort before returning provider list.
    - `internal/app/service.go` still returns errors directly when API calls fail.
    - `internal/app/service.go` still calls `FetchTemplate` with sorted final provider list.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app ./internal/api</automated>
  </verify>
  <done>Provider list, final selection, and template request providers are deterministic and validated by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → service | Untrusted provider keys enter selection path |
| Service → remote API | Untrusted network responses affect warnings and template content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `sanitizeKeys` / add+detect input path | mitigate | Keep supported-key validation before provider-set mutation; warn on unsupported keys and continue valid inputs only |
| T-02-02 | D | `internal/api/client.go` HTTP calls | mitigate | Preserve timeout and hard-fail on request/non-2xx errors; do not continue with partial/empty template data |
| T-02-03 | R | Deterministic ordering in outputs | mitigate | Sort provider lists before output/API request; add regression tests asserting stable ordering |
</threat_model>

<verification>
- Run `go test ./internal/app ./internal/api`.
- Confirm new tests explicitly map to CMD-05, DET-03, DET-04, API-01, API-02, API-03 behaviors.
</verification>

<success_criteria>
- Deterministic ordering assertions pass for provider-list and template request paths.
- Mixed valid/invalid provider input behavior is covered and stable.
- API failures remain hard-fail with no success-side file action.
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-selection-api-generation/02-01-SUMMARY.md`
</output>
