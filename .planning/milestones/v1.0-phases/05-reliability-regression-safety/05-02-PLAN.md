---
phase: 05-reliability-regression-safety
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - internal/provider/detectors.go
  - internal/provider/detectors_test.go
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/app/cli_test.go
  - internal/app/json_test.go
  - internal/app/types.go
autonomous: true
requirements: [TST-03, TST-04]
must_haves:
  truths:
    - "Selection semantics stay deterministic across detect reset, add append-only behavior, include/exclude resolution, and warning ordering"
    - "Detector regressions cover false positives and structured detector errors, including the Vue-on-Vite risk called out in phase context"
    - "CLI and JSON output contracts remain stable under success, warning, and failure paths"
  artifacts:
    - path: "internal/provider/detectors_test.go"
      provides: "Regression coverage for detector false positives, read/parse errors, and realistic signal combinations"
    - path: "internal/app/service_test.go"
      provides: "Deterministic selection, ordering, warning, and failure-path coverage"
    - path: "internal/app/cli_test.go"
      provides: "Human-readable output regressions for command summaries and verbose evidence boundaries"
    - path: "internal/app/json_test.go"
      provides: "Stable JSON payload assertions for command results and failure-safe machine output"
  key_links:
    - from: "internal/provider/detectors.go"
      to: "internal/app/service.go"
      via: "Detector results flow into final provider selection and output payloads"
      pattern: "Registry\(|DetectionResults|Detect\(ctx"
    - from: "internal/app/service.go"
      to: "internal/app/cli.go"
      via: "Service command results define both human-readable and JSON command contracts"
      pattern: "CommandResult|printResult"
---

<objective>
Finish Phase 5 by hardening end-to-end reliability around detector regressions, selection semantics, warning ordering, and output contracts so maintainers can trust the suite to catch user-facing regressions before release.

Purpose: Close the remaining gap between currently passing tests and the full shipped v1 contract for detection, selection, and automation-facing output.
Output: Expanded detector/service/CLI/JSON regressions plus any tightly scoped behavior fixes the new tests prove are necessary.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-reliability-regression-safety/05-CONTEXT.md

<interfaces>
From internal/app/service.go:
```go
func (s *Service) Detect(ctx context.Context, opts DetectOptions) (CommandResult, error)
func (s *Service) Add(ctx context.Context, opts AddOptions) (CommandResult, error)
```

From internal/app/types.go:
```go
type CommandResult struct {
  DetectedProviders      []string
  IncludedProviders      []string
  ExcludedProviders      []string
  AddedProviders         []string
  FinalProviders         []string
  UnsupportedKeyWarnings []string
  RemoteProviderWarnings []string
  DetectionResults       []provider.Result
  FileAction             gitignore.FileAction
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detector regressions for false positives and structured error handling</name>
  <files>internal/provider/detectors.go, internal/provider/detectors_test.go</files>
  <read_first>internal/provider/detectors.go, internal/provider/detectors_test.go</read_first>
  <behavior>
    - Test 1: Vue detection does not trigger solely from generic Vite config in a non-Vue project.
    - Test 2: React detection still matches only when `package.json` actually references React.
    - Test 3: File/path/app detectors preserve structured non-match and error metadata deterministically.
  </behavior>
  <action>Add focused detector regressions for the concrete false-positive risk called out in phase context and keep any implementation fix minimal. Do not broaden detection scope beyond current-directory v1 behavior.</action>
  <acceptance_criteria>
    - `internal/provider/detectors_test.go` includes a Vue-on-Vite regression case and continues to assert structured error results.
    - Any detector fix stays local to `internal/provider/detectors.go` and does not introduce a new detector framework.
    - `go test ./internal/provider` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/provider</automated>
  </verify>
  <done>Detector behavior is reliable enough that false positives and parse/read failures are no longer soft spots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand service regressions for deterministic selection and warning contracts</name>
  <files>internal/app/service.go, internal/app/service_test.go</files>
  <read_first>internal/app/service.go, internal/app/service_test.go, internal/provider/detectors.go</read_first>
  <behavior>
    - Test 1: `detect` resets to the detected-plus-included-minus-excluded final set with deterministic ordering.
    - Test 2: `add` remains append-only and never drops existing managed providers.
    - Test 3: Unsupported-key and remote-provider warnings are explicit and stably ordered.
    - Test 4: Failure paths still return before template fetch or file write when selection becomes invalid.
  </behavior>
  <action>Tighten service-level regressions so TST-03 is fully represented by exact slice and warning assertions. Only adjust service or shared result types if the new tests expose a real ordering or payload inconsistency.</action>
  <acceptance_criteria>
    - `internal/app/service_test.go` directly covers detect reset, add append-only, ordering, and warning determinism.
    - Warning assertions use exact ordered slices, not loose contains checks.
    - `go test ./internal/app -run "Test(Detect|Add)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(Detect|Add)"</automated>
  </verify>
  <done>Selection semantics and warnings are locked by tests rather than incidental implementation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish end-to-end output regressions for human and JSON contracts</name>
  <files>internal/app/cli_test.go, internal/app/json_test.go, internal/app/types.go</files>
  <read_first>internal/app/cli.go, internal/app/cli_test.go, internal/app/json_test.go, internal/app/types.go</read_first>
  <action>Broaden CLI and JSON tests to cover warning order, file action reporting, verbose evidence boundaries, and machine-readable payload stability under the realistic selection scenarios added in this phase. If the regressions expose a real result-shape or renderer bug, make the smallest compatible fix.</action>
  <acceptance_criteria>
    - `internal/app/cli_test.go` covers success paths with multiple warnings plus verbose/default evidence boundaries.
    - `internal/app/json_test.go` asserts stable field presence/order-sensitive slices for detect/add output.
    - `go test ./internal/app` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app</automated>
  </verify>
  <done>Human-readable and JSON outputs remain a dependable contract even under the broader regression matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem/runtime signals → detector matches | Untrusted local signals influence provider selection and can create false positives |
| Detector/service output → final provider set | Ordering and warning semantics become user-visible behavior and automation input |
| Command results → CLI/JSON output | Human and machine output must stay aligned under richer regression scenarios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S | Vue/Vite detector overlap | mitigate | Add explicit false-positive regression coverage and keep fix local to detector logic |
| T-05-05 | R | Selection and warning ordering | mitigate | Assert exact slices for final providers and warning order in service and output tests |
| T-05-06 | I | CLI/JSON output drift under richer scenarios | mitigate | Expand CLI and JSON contract tests using the same command result shapes |
</threat_model>

<verification>
- Run `go test ./internal/provider ./internal/app`.
- Confirm TST-03 and TST-04 are covered across detector, service, CLI, and JSON regressions.
</verification>

<success_criteria>
- Detector false positives and structured errors are explicitly covered.
- Selection semantics and warning ordering stay deterministic and test-backed.
- CLI and JSON outputs remain stable under the broader phase 5 regression matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-regression-safety/05-02-SUMMARY.md`
</output>
