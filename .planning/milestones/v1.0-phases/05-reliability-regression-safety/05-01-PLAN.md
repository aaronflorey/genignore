---
phase: 05-reliability-regression-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/client.go
  - internal/api/client_test.go
  - internal/api/testdata/list.json
  - internal/gitignore/manager.go
  - internal/gitignore/manager_test.go
  - internal/app/service_test.go
autonomous: true
requirements: [TST-01, TST-02]
must_haves:
  truths:
    - "Contributor can run API-dependent regression tests fully offline using checked-in fixtures and httptest"
    - "File migration scenarios are covered for missing files, files without markers, existing managed blocks, malformed markers, and dry-run safety"
    - "Equivalent reruns avoid unintended file churn once regression tests expose the real behavior gap"
  artifacts:
    - path: "internal/api/client_test.go"
      provides: "Offline fixture coverage for realistic provider-list and template API contracts"
    - path: "internal/gitignore/manager_test.go"
      provides: "Regression coverage for managed block migration, malformed marker failures, and no-op reruns"
    - path: "internal/app/service_test.go"
      provides: "Command-level guarantees that API failures and dry-runs do not write .gitignore"
  key_links:
    - from: "internal/app/service_test.go"
      to: "internal/gitignore/manager.go"
      via: "Service file-action paths depend on manager write/no-write behavior"
      pattern: "Detect\(|Add\(|UpsertManagedBlock"
    - from: "internal/api/client.go"
      to: "internal/api/testdata/list.json"
      via: "Provider-list decoding must match the checked-in fixture contract used by offline tests"
      pattern: "AvailableProviders"
---

<objective>
Lock the offline and file-safety half of Phase 5 by making API fixtures realistic, proving `.gitignore` migration behavior end to end, and fixing any real no-op churn or boundary bugs that those regressions expose.

Purpose: Turn the current mostly unit-level coverage into a trustworthy offline regression baseline for API and file mutation behavior.
Output: Expanded offline fixture tests, stronger manager/service regressions, and any small implementation fixes needed to eliminate real file-safety gaps.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-reliability-regression-safety/05-CONTEXT.md

<interfaces>
From internal/api/client.go:
```go
func (c *Client) AvailableProviders(ctx context.Context) ([]string, error)
func (c *Client) FetchTemplate(ctx context.Context, providers []string) (TemplateResponse, error)
```

From internal/gitignore/manager.go:
```go
func (m *Manager) ReadManagedProviders() ([]string, error)
func (m *Manager) UpsertManagedBlock(block string, dryRun bool) (FileAction, error)
func BuildManagedBlock(providers []string, template string) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand offline API fixture coverage to match the real provider-list contract</name>
  <files>internal/api/client.go, internal/api/client_test.go, internal/api/testdata/list.json</files>
  <read_first>internal/api/client.go, internal/api/client_test.go, internal/api/testdata/list.json</read_first>
  <behavior>
    - Test 1: Provider-list decoding succeeds against a realistic checked-in fixture rather than a toy shape.
    - Test 2: Provider-list results remain alphabetically sorted and deterministic across repeated calls.
    - Test 3: Non-2xx list/template responses still hard-fail with stable error behavior.
  </behavior>
  <action>Strengthen the API fixture and client tests so phase 5 no longer relies on an overly convenient list response shape. Keep all API tests offline with `httptest`, and only change decoding logic if the realistic fixture proves the current contract handling is incomplete.</action>
  <acceptance_criteria>
    - `internal/api/client_test.go` covers realistic list decoding and failure paths without live network access.
    - `internal/api/testdata/list.json` reflects the contract shape the client is expected to tolerate in v1.
    - `go test ./internal/api` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/api</automated>
  </verify>
  <done>API-dependent regression tests are offline-safe and realistic enough to catch contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add file migration and no-op regression coverage for managed block safety</name>
  <files>internal/gitignore/manager.go, internal/gitignore/manager_test.go</files>
  <read_first>internal/gitignore/manager.go, internal/gitignore/manager_test.go</read_first>
  <behavior>
    - Test 1: Missing-file, no-marker, managed-marker, and malformed-marker scenarios remain explicitly covered.
    - Test 2: Content outside the managed block, including leading blank lines, remains byte-exact.
    - Test 3: Equivalent reruns prove whether the manager rewrites unnecessarily, and the implementation is tightened if the test exposes churn.
    - Test 4: Dry-run on an existing file preserves the file byte-for-byte.
  </behavior>
  <action>Broaden manager regressions to cover the exact file-boundary guarantees called out in the phase context, then make the smallest implementation change required if a no-op rerun or boundary case still rewrites when it should not.</action>
  <acceptance_criteria>
    - `internal/gitignore/manager_test.go` explicitly covers the shipped migration scenarios for TST-02.
    - Any implementation change stays inside the existing manager boundary rather than adding a new abstraction.
    - `go test ./internal/gitignore` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/gitignore</automated>
  </verify>
  <done>Managed block file behavior is regression-proven instead of inferred from a few happy-path tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock service-level failure and dry-run non-write behavior</name>
  <files>internal/app/service_test.go</files>
  <read_first>internal/app/service.go, internal/app/service_test.go, internal/gitignore/manager.go</read_first>
  <action>Extend service tests so list/template API failures, dry-run paths, and existing-file scenarios explicitly prove that `.gitignore` is not mutated on failure and remains unchanged during dry-run. Reuse the current fake API seam and temp directories; only touch service code if the tighter regressions expose a real write-side bug.</action>
  <acceptance_criteria>
    - `internal/app/service_test.go` covers no-write behavior on API failure and dry-run for both missing and existing files.
    - Tests assert file state directly rather than assuming behavior from return values alone.
    - `go test ./internal/app -run "Test(API|Template|DryRun|Detect|Add)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run "Test(API|Template|DryRun|Detect|Add)"</automated>
  </verify>
  <done>The command layer has explicit regression proof that failures and dry-runs do not write user files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote API contract → offline fixture tests | Checked-in fixtures must be realistic enough to catch contract drift without depending on live network calls |
| Service/template output → `.gitignore` writes | API and template success paths must never silently mutate files on failures or dry-runs |
| Existing `.gitignore` content → managed block merge | User-owned content outside the managed region must remain byte-exact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | R | API list fixture realism | mitigate | Replace toy fixture assumptions with realistic checked-in contract coverage |
| T-05-02 | T | Managed block merge/no-op behavior | mitigate | Add byte-exact migration and rerun tests; tighten manager logic only if tests expose churn |
| T-05-03 | D | Failure-path file writes | mitigate | Assert no-write behavior directly in service tests for API failure and dry-run paths |
</threat_model>

<verification>
- Run `go test ./internal/api ./internal/gitignore ./internal/app -run 'Test(API|Template|DryRun|Detect|Add|Upsert|Client)'`.
- Confirm TST-01 and TST-02 are covered by offline API, manager, and service regressions.
</verification>

<success_criteria>
- API-dependent regression tests run fully offline using realistic fixtures.
- File migration scenarios are covered with explicit byte-preservation assertions.
- Failure and dry-run paths prove `.gitignore` is not written when it should not be.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-regression-safety/05-01-SUMMARY.md`
</output>
